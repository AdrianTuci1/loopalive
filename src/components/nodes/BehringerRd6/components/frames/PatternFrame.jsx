import React from 'react';

const PatternFrame = ({ children }) => (
  <div className="rd6-pattern-group">
    <div className="rd6-group-label">PATTERN GROUP</div>
    {children}
  </div>
);

export default PatternFrame; 