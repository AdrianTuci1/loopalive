import React from 'react';

const VolumeFrame = ({ children }) => (
  <div className="rd6-volume-frame">
    <div className="rd6-volume-label">VOLUME</div>
    {children}
  </div>
);

export default VolumeFrame; 